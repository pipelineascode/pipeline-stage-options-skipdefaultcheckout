pipeline {
    agent any

    stages {
        stage('Build') {

			options { 
				skipDefaultCheckout() 
			}		

            steps {                
                echo 'Hello World 1'
            }
        }
    }
}
